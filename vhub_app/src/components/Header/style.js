import styled from "styled-components";

export const HeaderUserProfile = styled.View`
    flex-direction: row;
    align-items: center;
    width: 55%;
    gap: 10px;
`

export const HeaderUserProfileData = styled(HeaderUserProfile)`
    gap: 3px;
    flex-direction: column;
    align-items: flex-start;
`
